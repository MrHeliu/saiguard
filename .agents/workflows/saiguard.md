---
description:
---

1. On session start, auto-execute `sai status` and show the output in your reply
2. When a code modification is requested but no task is in doing state, remind to run `sai start` first before proceeding
